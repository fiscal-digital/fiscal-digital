{
  "name": "@fiscal-digital/collector",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda coletora — integração com Querido Diário API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/index.js --external:@aws-sdk/*",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fiscal-digital/engine": "*",
    "@aws-sdk/client-sqs": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/aws-lambda": "^8.0.0",
    "typescript": "^5.0.0",
    "esbuild": "^0.20.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
