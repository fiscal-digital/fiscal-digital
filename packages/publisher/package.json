{
  "name": "@fiscal-digital/publisher",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda publicadora — Lineu posta alertas no X e Reddit",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild src/index.ts --bundle --platform=node --target=node24 --outfile=dist/index.js --external:@aws-sdk/*",
    "test": "jest --passWithNoTests",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fiscal-digital/engine": "*",
    "twitter-api-v2": "^1.15.0",
    "snoowrap": "^1.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/aws-lambda": "^8.0.0",
    "typescript": "^6.0.0",
    "esbuild": "^0.28.0",
    "jest": "^30.0.0",
    "@types/jest": "^30.0.0",
    "ts-jest": "^29.4.0"
  }
}
